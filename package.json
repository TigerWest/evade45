{
  "name": "dead-air",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173 --bind 127.0.0.1 --directory dist",
    "test": "node --test tests/*.test.js",
    "check": "node --check dist/game.js && node --check dist/engine.js && node --check dist/render.js && node --check dist/audio.js",
    "build": "node scripts/build.mjs"
  },
  "dependencies": {
    "three": "0.186.0"
  }
}
